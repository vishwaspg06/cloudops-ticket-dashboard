apiVersion: v1
kind: Service
metadata:
  name: cloudops-backend-service
spec:
  selector:
    app: cloudops-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP