apiVersion: v1
kind: Service
metadata:
  name: cloudops-frontend-service
spec:
  selector:
    app: cloudops-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP